export interface ToDoItem {
  task: string;
  completed: boolean;
  creationDate: string;
  dueDate: string;
}

export interface FullToDoItem {
  id: number;
  task: string;
  completed: boolean;
  creationDate: string;
  dueDate: string;
}
